<h3> Meus Projetos: <h3>


<%if @projects.empty? %> 
   <h3> Você ainda não possui projetos</h3>
<%end%>

<%if @open_projects.any? %>    
 <h4> Projetos em aberto </h4> 
   <%@open_projects.each do |p|%>
   <%= link_to p.title, project_path(p.id)%>
   <br>
   <%end%>
<%end%>  

<%if @full_projects.any? %>    
 <h4> Projetos com colaboradores completos </h4> 
   <%@full_projects.each do |p|%>
   <%= link_to p.title, project_path(p.id)%>
   <br>
   <%end%>
<%end%>  

<%if @running_projects.any? %>    
 <h4> Projetos em andamento </h4> 
   <%@running_projects.each do |p|%>
   <%= link_to p.title, project_path(p.id)%>
   <br>
   <%end%>
<%end%>  

<%if @closed_projects.any? %>    
 <h4> Projetos concluídos </h4> 
   <%@closed_projects.each do |p|%>
   <%= link_to p.title, project_path(p.id)%>
   <br>
   <%end%>
<%end%>  


       
