<h3> Meus Projetos: <h3>

<%if @projects.empty? %> 
   <h3> Você ainda não possui projetos</h3>
<%else%>       
   <%@projects.each do |p|%>
   <%= link_to p.title, project_path(p.id)%>
   <br>
   <%end%>

<%end%>  
